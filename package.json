{
  "name": "backend-pos",
  "version": "1.0.0",
  "description": "Backend POS pirotecnia con Express, Sequelize y MySQL",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/server.js",
    "build": "npm run build --prefix ../frontend-pos && cd ../ && cp -r frontend-pos/build/ backend-pos/public && cd backend-pos",
    "deploy": "npm install",
    "start": "node src/server.js",
    "sync": "node src/app.js --sync"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^9.0.1",
    "morgan": "^1.10.1"
  }
}
